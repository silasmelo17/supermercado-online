
import Authentication from "../../types/Authentication";
import { INITIAL_AUTH_STATE } from "../initialState";



function authenticationReducer( state:Authentication = INITIAL_AUTH_STATE ) {
    return state;
}

export default authenticationReducer;
